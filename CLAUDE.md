你现在看到的项目是一个基于“逐步精化”思想的人机协同编程 VS Code 插件。该项目的前端组件包含了一个 TreeView 和三个 WebView，其中三个 WebView 的 html、css 和 js 代码位于 /html 目录下，包括 operation-panel、design-tree-view 和 division-plan-view。下面从整体的流程出发，介绍整个插件的设计和主要编码：首先，用户在左侧的 TreeView 中“创建项目”，此时会出现一个项目根节点，内部自带一个 Project Requirement 节点；接着，用户点开 Project Requirement 节点，在编辑器中显示对应的文件，并写入项目的整体需求；之后，用户右击项目根节点，并选择“加载到工作区”，之后会在右侧显示两个 WebView（operation-panel 和 designmen-tree-view），其中 designment-tree-view 用 d3 以树状图的形式展示了项目进行模块划分的层级结构（项目第一次加载时应该只有一个根节点），而 operation-panel 则展示了项目当前的叶子节点基于依赖关系的拓扑序列，以及”精化历史“。

* 整个项目采用”先进行模块划分，再逐个模块进行‘精化’”的思想。精化面板中有三个按钮：拆分、精化和保存工作区。用户可以选择设计树中的一个叶子节点（初始时是根节点），并点击“拆分”按钮，之后后端会调用大模型将该节点的需求拆分到多个子模块。当项目进行第一次拆分过后（不只是有一个单独的叶子节点后），设计树的叶子节点构成了当前整个项目的当前划分状态，保证这些叶子节点之间形成的依赖图具有拓扑顺序。而操作面板中会再“叶子拓扑序列”中维护一个叶子节点的拓扑顺序列表，该列表中的节点按照拓扑顺序排列且和设计树中的叶子模块一一对应，且目前点击叶子模块和点击拓扑顺序中的节点会产生联动效果，即点击一个节点或模块，在另一张图中的对应节点也会被选中。点击设计树中的每一个节点都会显示该节点对应的内容文件，特别的，点击叶子节点（或叶子拓扑序列中的节点）后，除了在编辑器中显示对应的内容文件，还会在下方的“精化历史”栏中显示该节点的精化历史，在开始精化之前每个叶子节点的精化历史中都有一个初始的“模块规约”节点，该节点对应的内容文件也就是设计树中该节点对应的内容文件，二者是一样的。
* 将项目划分为一定的模块之后，用户可以**按照拓扑顺序**，一个一个地对叶子模块进行“精化与代码生成”。这个过程就是从最初的模块规约出发，先生成伪代码，后生成实际代码地过程。用户首先选中一个叶子模块（上面提到此时会显示精化历史），之后选择一种精化方式（“全局精化”或”局部精化“，全局精华指根据精化历史中的最后一个节点的全部内容生成更详细的伪代码，而局部精化则需要用户从最后节点的文件中选中一部分内容，然后针对该部分内容进行精化，其它部分不变）进行精化，最后选择”代码生成“，即生成该模块的实际代码（目前默认仅支持 python）；代码生成过后，该模块的精化与实现流程算告一段落，之后用户需要继续精化并实现拓扑顺序中的下一个模块，直到所有模块的代码全部生成。**一定要注意用户只能按照依赖拓扑顺序一个一个模块地精化、生成**。
  * 特别的，在开始对第一个模块进行精化或生成时，需要首先进行”提取通用数据结构“的操作，该操作会在设计树中添加一个”通用数据结构“节点。在对第一个模块进行代码生成时，还需要先生成”实际数据结构“，即生成固定语言的实际数据结构代码。
  * 可以直接对第一个模块进行代码生成而不进行精化，此时就要同时提取通用数据结构和生成实际数据结构。
* 用户能够在开始精化后再次点击设计树中的叶子节点，并选择”划分“。此时会弹出一个弹窗提醒用户，检测到当前已有精化历史，继续划分会先清空所有精化历史，是否继续？用户如果选择是，则会清空精化历史（包括生成的通用数据结构和实际数据结构）并重新划分该叶子模块，之后会形成新的拓扑顺序，用户需要重新从第一个模块开始进行精化与生成。
* 精化开始后，用户目前还实现了一个”精化回退“的功能。你可以这样建模，用户在划分模块完成后，一个模块一个模块地精化和生成代码的过程是一个增量式的过程，会从第一个叶子模块开始，依次形成一个从模块规约到实际代码的链条，这些链条中的每一个节点都有一个明确的先后关系。用户可以选择一个已经精化或生成的叶子模块，在其精化历史中选择一个节点，并点击”回退到该节点“，此时会删除所有在它之后生成的精化历史节点（包括拓扑顺序中位于该模块之后的节点的精化链条，以及同模块内部在其后生成的节点，当然每个模块最初的模块规约不该被删除）。
  * 回退时不应该删除通用数据结构或实际数据结构，重新生成时需要检测是否以存在这两个对应文件，之后再决定是否要生成。
* **重要**：现在的设计是”TreeView 代表实际项目目录，而操作面板和设计树代表工作区“。用户点击”将项目加载到工作区“后，会将项目当前的实际情况加载到工作区显示出来，之后用户在操作面板上的一切操作（除了“保存工作区”之外）都只会写入内存或者一个 .tmp 目录，包括划分、精化和代码生成、回退等一系列操作，只有当用户点击“保存工作区”之后，才会导致工作区的内容被同步到实际项目目录。这一点需要你好好地理解，后面我们要做比较大的修改。

我会指出项目中核心代码的位置：

* 前端代码：`/html` 目录。
* TreeView：`/src/designment-tree-view/designment-tree-provider.ts`。
* operation-panel：`/src/operation-panel-view/**`。

你可以从这些文件开始理解整个项目结构，也可以随意地读取任何文件以增进你的理解。请保证你完全理解了上面的**旧项目结构**之后再开始动手。

基于现在的版本，我们要对项目进行一次**较大的调整**：

* 前面我们已经知道，现在的设计树和操作面板都是工作区。但是根据新的需求，我们现在只需要让**设计树**（即划分操作）是工作区就行了，而操作面板（即所有精化和代码生成操作）上的行为则直接修改原始项目目录，不需要先写如 .tmp 目录。这从某种程度上来说是简化，但需要一定程度的重构。
* UI 的变化：整体没有太大的变化，但是我们要将操作面板中的“划分”和“保存”按钮移到设计树中。但是设计树中似乎没有适合用于容纳按钮的位置，因此我像这样做：当用户的鼠标悬停在某个节点叶子节点（或单独的根节点）上时，在节点的旁边会显现一个竖着的按钮框，其中包括”划分“这一选项。你应该可以看到现在的设计树右上角实现了一个用于放大缩小的按钮框，样式应该和那个类似。同时，我们后面还要实现别的按钮，暂时就先添加”划分“这一个操作，但注意**保证可扩展性**。划分的逻辑和之前一样就行，后端调用 LLM 划分并提供备选方案（division-plan-view），用户选择后先**写入暂存区**然后将变化更新到设计树上（不应该影响其它实际项目目录或者操作面板）。而“保存”按钮则以同样的样式添加到页面的右下角，此时“保存”的行为仅仅只是同步模块划分结果，将 .tmp 中的项目结构写入实际项目目录，不应该与精化历史产生关系。
* 至于操作面板：UI 上把划分按钮和保存按钮砍掉，且其中的内容始终与项目的实际目录同步。当项目加载时，会把项目当前的叶子模块和精化历史加载进来，之后在设计树中的划分操作应该只影响工作区，而不应该影响操作面板；但是，当用户在设计树中点击”保存“之后，如果模块划分结构被改变，那么项目实际目录将被改变，此时如果检测到有精化历史，应该弹出弹框提醒“检测到已存在精化历史，如修改设计，会导致精化历史被清空。是否继续？”，用户如果选择是，那么就会清空所有的精化历史（包括通用和实际数据结构），用户需要从第一个叶子模块重新开始精化与生成。另外，再强调一下，精化与生成操作的行为和以前一样，但是**不需要先写入 .tmp 保存，直接修改实际项目目录即可；精化回退也是一样**。也就是说，精化与生成操作与暂存不再有任何关系。

这次任务可能稍微有些复杂，请务必确保你理解了旧项目的流程、代码结构和新的需求之后再进行细致的规划，再开始编码。如果有任何问题，可以向我确认，保证你完全理解了要做什么，没有模糊的地方。